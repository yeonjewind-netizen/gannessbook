// TODO: [Phase 2] 이 고래 도약 및 회고 로직은 추후 '목표 달성 전용 페이지' 라우팅이 구현되면 그쪽으로 이동시킬 것.

/**
 * 마이페이지에서는 목표 달성 연출(항해 완료 버튼·모달)을 숨깁니다.
 * Phase 2 전용 페이지에서 `SHOW_VOYAGE_COMPLETION_ON_MYPAGE`를 `true`로 바꾸거나,
 * 동일 컴포넌트(`VoyageCompletionModal` 등)를 달성 페이지에서 import 해 사용하면 됩니다.
 */
export const SHOW_VOYAGE_COMPLETION_ON_MYPAGE = false
